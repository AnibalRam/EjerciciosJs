let sumaInterna = function (array) {
  
  contador = 0
  for (i= 0; i<array.length; i++){
  contador = (contador + array[i]) 
  }
}

let NewArray= [2, 4, 5, 1, 2]
sumaInterna(NewArray)
let SumaTotal = (contador+ NewArray.length)

console.log(` La suma de los elementos del array y su largo es de es de ${SumaTotal} `)

